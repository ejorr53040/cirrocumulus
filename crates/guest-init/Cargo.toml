[package]
name = "guest-init"
edition.workspace = true
version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
nix = { workspace = true, features = ["mount", "process", "signal", "reboot", "fs", "net"] }
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"
